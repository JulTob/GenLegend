# Python 3.10+ (3.14 in production). Web app: Shiny only. `make setup` installs into .venv.
shiny==1.6.3

# Tag-Oriented Programming: TopKit (formerly TagKit), the reference implementation,
# pinned to its first PyPI release, 0.2.0a3 (2026-09-21). The distribution is
# `topkit` and the module is `TopKit`. Moved from the TagKit 0.2.0a1 git pin by
# QST-0093.5; the upstream issues found on the way are QST-0093.10.
topkit==0.2.0a3
